{{# partial "titulo" }}
    <title>Tramo</title>
{{/partial}}

{{# partial "contenido" }}
    <section>
        <form method="POST" action="/home/sector/{{sectorId}}/trayecto/{{trayectoId}}/tramo/{{tramoId}}/transportePublico">
            Completar datos del tramo por transporte público
            <div class="tipo-transporte-publico">
               {{#each transportesPublicos}}
                   <div class="tipo-consumo">
                       <input type="radio" id="{{id}}" name="transportePublico" value="{{id}}">
                       <label for="{{id}}">{{nombre}}</label>
                   </div>
               {{/each}}
            </div>
           <div class="text-center">
             <input type="submit" class="btn-borde" value="Siguiente" />
           </div>
        </form>
    </section>
{{/partial}}

{{> layout.html.hbs }}